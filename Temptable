



⸻

– GLOBAL TEMP TABLE
– RUN ONLY ONCE

CREATE GLOBAL TEMPORARY TABLE TEMP_GENERATED_DATA
(
CIRCLE_CODE        VARCHAR2(20),
BATCH_ID           NUMBER,
TRANSACTION_COUNT  NUMBER,
BRANCH_CODE        VARCHAR2(20),
CURRENCY           VARCHAR2(10),
CGL                VARCHAR2(20),
CREDIT_AMOUNT      NUMBER(25,4),
DEBIT_AMOUNT       NUMBER(25,4)
)
ON COMMIT DELETE ROWS;
/

⸻

– OPTIONAL INDEXES

CREATE INDEX IDX_TEMP_GEN_1
ON TEMP_GENERATED_DATA (BRANCH_CODE);

CREATE INDEX IDX_TEMP_GEN_2
ON TEMP_GENERATED_DATA (CGL);

CREATE INDEX IDX_TEMP_GEN_3
ON TEMP_GENERATED_DATA (CIRCLE_CODE);
/
